syntax = "proto3";

package groceries.v1;

import "groceries/v1/items.proto";

// Category management.
service CategoryService {
  // List all categories.
  rpc ListCategories(ListCategoriesRequest) returns (ListCategoriesResponse);

  // Create a new category.
  rpc CreateCategory(CreateCategoryRequest) returns (CreateCategoryResponse);

  // Get a category by ID, including its items.
  rpc GetCategory(GetCategoryRequest) returns (GetCategoryResponse);

  // Update a category.
  rpc UpdateCategory(UpdateCategoryRequest) returns (UpdateCategoryResponse);

  // Delete a category.
  //
  // Fails with FAILED_PRECONDITION if any items are still assigned to the
  // category.
  rpc DeleteCategory(DeleteCategoryRequest) returns (DeleteCategoryResponse);
}

message Category {
  int64 id = 1;
  int64 store_id = 2;
  string name = 3;
  string description = 4;

  // Number of items assigned to this category.
  int64 item_count = 5;
}

message ListCategoriesRequest {}

message ListCategoriesResponse {
  repeated Category categories = 1;
}

message CreateCategoryRequest {
  int64 store_id = 1;
  string name = 2;
  string description = 3;
}

message CreateCategoryResponse {
  Category category = 1;
}

message GetCategoryRequest {
  int64 id = 1;
}

message GetCategoryResponse {
  Category category = 1;
  repeated Item items = 2;
}

message UpdateCategoryRequest {
  int64 id = 1;
  int64 store_id = 2;
  string name = 3;
  string description = 4;
}

message UpdateCategoryResponse {
  Category category = 1;
}

message DeleteCategoryRequest {
  int64 id = 1;
}

message DeleteCategoryResponse {}
